import { Meta, Stories } from '@storybook/addon-docs/blocks';
import * as DocStories from './Toast.stories';

<Meta of={DocStories} />

# Toast

Toast notifications are temporary, non-modal status updates used to inform users about the outcome of an action or system state.

```tsx
import { ToastContainer, toast } from '@geti/ui';
```

- [React Spectrum ToastContainer](https://react-spectrum.adobe.com/react-spectrum/ToastContainer.html)

<Stories />

## Responsibility

Provides the complete toast notification system: a mount-point component (`ToastContainer`) that renders queued toasts in the DOM, and a programmatic imperative API (`toast`) for enqueuing notifications from anywhere in the application without needing component context.

## When to use

- **Success Confirmation**: To notify a user that an action (e.g., saving a project, deleting an item) completed successfully.
- **Error Alerts**: To warn a user about a failed process or network error that doesn't block the entire workflow.
- **Informational Updates**: To provide non-critical context about background processes.
- **Async Feedback**: To confirm when a long-running background task has finished.

## When to avoid

- **Critical Warnings**: If a user must acknowledge a message before proceeding, use a `Dialog` or `AlertDialog` instead.
- **Lengthy Content**: Toasts should be concise. For detailed logs or long messages, use a dedicated notification center or status page.
- **Persistent Information**: Toasts disappear automatically; do not use them for information the user needs to refer back to later.

## Design

Two exports with different usage models:

**`ToastContainer`** — thin wrapper over Spectrum's `ToastContainer`. Must be rendered once in the application tree (typically near the root). Accepts the same props as `SpectrumToastContainer`.

**`toast`** — a plain object (not a component) with four methods:

```ts
toast.positive(message, options?)
toast.negative(message, options?)
toast.info(message, options?)
toast.neutral(message, options?)
```

Each method calls `SpectrumToastQueue.<variant>(message, options)` directly. This gives callers a stable, variant-named API without importing Spectrum internals. The `toast` object is a pure function dispatch table — no hooks, no React lifecycle.

The separation of `ToastContainer` (render tree) from `toast` (imperative API) follows the "render once, call anywhere" pattern, enabling toast creation from event handlers, async callbacks, and non-React code.

## Integration

- `ToastContainer` must be present in the React tree for toasts to appear.
- `toast.*` can be called from any module — event handlers, async utilities, error boundaries — without component imports.
- Spectrum manages toast queuing, deduplication, and auto-dismiss internally.

**Setup (once)** — render `ToastContainer` in the application root:

```tsx
// App root
<ToastContainer />
```

**Usage (anywhere)** — call `toast.*` from any module:

```tsx
toast.negative('Upload failed', { timeout: 5000 });
```

## Props

### ToastContainer Props

`ToastContainer` accepts the same props as `SpectrumToastContainer` — no additional props are added by the Geti wrapper.

### ToastOptions

| Property              | Type         | Default | Description                                                                   |
| :-------------------- | :----------- | :------ | :---------------------------------------------------------------------------- |
| `timeout`             | `number`     | `5000`  | Time in milliseconds before the toast automatically dismisses.                |
| `actionLabel`         | `string`     | —       | Label for an optional action button within the toast.                         |
| `onAction`            | `() => void` | —       | Callback triggered when the action button is clicked.                         |
| `shouldCloseOnAction` | `boolean`    | `true`  | Whether the toast should dismiss automatically after the action is triggered. |

## Accessibility

Toasts are rendered in an ARIA live region to ensure they are announced by screen readers immediately upon appearing.

| Feature         | Implementation                                                                                             |
| :-------------- | :--------------------------------------------------------------------------------------------------------- |
| **Role**        | Uses `role="status"` or `role="alert"` depending on the variant (negative uses alert).                     |
| **Live Region** | `aria-live="polite"` for most toasts; `aria-live="assertive"` for critical errors.                         |
| **Focus**       | Toasts do not take focus on appearance to avoid interrupting the user's flow.                              |
| **Keyboard**    | Users can navigate to active toasts via `F6` (standard Spectrum behavior) to interact with action buttons. |

## Related components

- **`InlineAlert`** — use instead of `Toast` when the message must remain visible until explicitly dismissed or until the triggering condition changes (e.g. a form validation error).
- **`Dialog` / `AlertDialog`** — use when the user must acknowledge a critical message before continuing; toasts are non-blocking and non-modal.
- **`StatusLight`** — use to show the ongoing operational state of a resource; unlike `Toast`, it does not disappear after a timeout.
- **`Badge`** — use for static categorical metadata attached to an entity (e.g. "Beta", "Archived") rather than for event-driven feedback.

## Example use cases

<details>
  <summary>Typical use cases</summary>

- Confirming that a model deployment succeeded after the user clicks "Deploy" — `toast.positive('Model deployed successfully.')`.
- Alerting the user that a background export failed — `toast.negative('Export failed. Check your storage quota.')`.
- Notifying the user that a long-running annotation job has finished — `toast.info('Auto-labelling complete. Review the results.')`.
- Confirming that a dataset was deleted — `toast.positive('Dataset deleted.', { actionLabel: 'Undo', onAction: handleUndo })`.
- Warning that the current browser session is about to expire — `toast.neutral('Your session expires in 5 minutes.', { timeout: 0 })`.

</details>
