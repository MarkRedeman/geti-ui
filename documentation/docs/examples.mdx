import { View, Flex, Divider, Button, ActionButton, ToggleButton, TagGroup, TagItem, ListView, ListItem, Badge, StatusLight, SearchField, ComboBox, ComboBoxItem, DatePicker, ProgressBar, InlineAlert, Tabs, TabList, TabPanels, TabItem as Item, DialogTrigger, Popover, Dialog } from '@geti/ui';
import { Heading, Text, Content } from '@adobe/react-spectrum';
import { ExampleCard } from '../theme/ExampleCard';

# Examples

Use this page as a **composition gallery** for `@geti/ui`.

Unlike component reference pages (one component at a time), this page focuses on
larger UI building blocks and how components fit together.

## Kitchensink

<ExampleCard
  title="Kitchensink"
  code={`<View borderWidth="thin" borderColor="dark" borderRadius="regular" padding="size-250" width="size-6000">
  <Flex direction="column" gap="size-200">
    <Heading level={4} margin={0}>UI primitives</Heading>
    <Flex gap="size-100" wrap>
      <Button variant="accent">Create</Button>
      <Button variant="secondary" style="outline">Cancel</Button>
      <ActionButton aria-label="Refresh">↻</ActionButton>
      <ToggleButton>Auto-refresh</ToggleButton>
      <Badge variant="info">Running</Badge>
      <StatusLight variant="positive">Healthy</StatusLight>
    </Flex>

    <Divider />

    <Heading level={4} margin={0}>Form controls + pickers</Heading>
    <Flex direction="column" gap="size-150" width="size-3600">
      <SearchField aria-label="Search assets" placeholder="Search assets" />
      <ComboBox label="Model" items={[{ id: 'm1', name: 'Vehicle detector' }, { id: 'm2', name: 'Person detector' }]}>
        {(item) => <ComboBoxItem>{item.name}</ComboBoxItem>}
      </ComboBox>
      <DatePicker label="Created after" />
    </Flex>

    <Divider />

    <Heading level={4} margin={0}>Data display</Heading>
    <ListView aria-label="Media list" width="size-3600" selectionMode="multiple" items={[{ id: '1', title: 'frame_001.jpg', tags: ['car', 'road'] }, { id: '2', title: 'frame_002.jpg', tags: ['person'] }] }>
      {(item) => (
        <ListItem textValue={item.title}>
          <Flex direction="column" gap="size-75">
            <Text>{item.title}</Text>
            <TagGroup aria-label="Item tags">
              {item.tags.map((tag) => <TagItem key={tag}>{tag}</TagItem>)}
            </TagGroup>
          </Flex>
        </ListItem>
      )}
    </ListView>

    <Divider />

    <Heading level={4} margin={0}>Navigation + overlays</Heading>
    <Tabs aria-label="Workspace">
      <TabList>
        <Item key="dataset">Dataset</Item>
        <Item key="models">Models</Item>
        <Item key="settings">Settings</Item>
      </TabList>
      <TabPanels>
        <Item key="dataset"><Content>Dataset content</Content></Item>
        <Item key="models"><Content>Model content</Content></Item>
        <Item key="settings"><Content>Settings content</Content></Item>
      </TabPanels>
    </Tabs>

    <DialogTrigger>
      <Button variant="secondary" style="outline">Open details</Button>
      <Popover>
        <Dialog>
          <Heading>Review details</Heading>
          <Text>Open the task panel to review low-confidence predictions.</Text>
        </Dialog>
      </Popover>
    </DialogTrigger>

    <Divider />

    <Heading level={4} margin={0}>Status + feedback</Heading>
    <Flex direction="column" gap="size-150" width="size-3600">
      <ProgressBar label="Training progress" value={62} />
      <InlineAlert variant="notice" title="Review required">
        Some predictions need manual confirmation.
      </InlineAlert>
    </Flex>
  </Flex>
</View>`}
>
  <View borderWidth="thin" borderColor="dark" borderRadius="regular" padding="size-250" width="size-6000">
    <Flex direction="column" gap="size-200">
      <Heading level={4} margin={0}>UI primitives</Heading>
      <Flex gap="size-100" wrap>
        <Button variant="accent">Create</Button>
        <Button variant="secondary" style="outline">Cancel</Button>
        <ActionButton aria-label="Refresh">↻</ActionButton>
        <ToggleButton>Auto-refresh</ToggleButton>
        <Badge variant="info">Running</Badge>
        <StatusLight variant="positive">Healthy</StatusLight>
      </Flex>

      <Divider />

      <Heading level={4} margin={0}>Form controls + pickers</Heading>
      <Flex direction="column" gap="size-150" width="size-3600">
        <SearchField aria-label="Search assets" placeholder="Search assets" />
        <ComboBox
          label="Model"
          items={[
            { id: 'm1', name: 'Vehicle detector' },
            { id: 'm2', name: 'Person detector' },
          ]}
        >
          {(item) => <ComboBoxItem>{item.name}</ComboBoxItem>}
        </ComboBox>
        <DatePicker label="Created after" />
      </Flex>

      <Divider />

      <Heading level={4} margin={0}>Data display</Heading>
      <ListView
        aria-label="Media list"
        width="size-3600"
        selectionMode="multiple"
        items={[
          { id: '1', title: 'frame_001.jpg', tags: ['car', 'road'] },
          { id: '2', title: 'frame_002.jpg', tags: ['person'] },
        ]}
      >
        {(item) => (
          <ListItem textValue={item.title}>
            <Flex direction="column" gap="size-75">
              <Text>{item.title}</Text>
              <TagGroup aria-label="Item tags">
                {item.tags.map((tag) => <TagItem key={tag}>{tag}</TagItem>)}
              </TagGroup>
            </Flex>
          </ListItem>
        )}
      </ListView>

      <Divider />

      <Heading level={4} margin={0}>Navigation + overlays</Heading>
      <Tabs aria-label="Workspace">
        <TabList>
          <Item key="dataset">Dataset</Item>
          <Item key="models">Models</Item>
          <Item key="settings">Settings</Item>
        </TabList>
        <TabPanels>
          <Item key="dataset"><Content>Dataset content</Content></Item>
          <Item key="models"><Content>Model content</Content></Item>
          <Item key="settings"><Content>Settings content</Content></Item>
        </TabPanels>
      </Tabs>

      <DialogTrigger>
        <Button variant="secondary" style="outline">Open details</Button>
        <Popover>
          <Dialog>
            <Heading>Review details</Heading>
            <Text>Open the task panel to review low-confidence predictions.</Text>
          </Dialog>
        </Popover>
      </DialogTrigger>

      <Divider />

      <Heading level={4} margin={0}>Status + feedback</Heading>
      <Flex direction="column" gap="size-150" width="size-3600">
        <ProgressBar label="Training progress" value={62} />
        <InlineAlert variant="notice" title="Review required">
          Some predictions need manual confirmation.
        </InlineAlert>
      </Flex>
    </Flex>
  </View>
</ExampleCard>

<Divider marginTop="size-250" marginBottom="size-200" />

> More curated scenario pages are available in the Examples sidebar and will be
> expanded over time.
