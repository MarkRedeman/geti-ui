---
title: Theming
---

# Charts Theming

`@geti-ai/charts` ships its own theme layer built on top of Geti design tokens. It integrates with `ThemeProvider` from `@geti-ai/ui` but can also be used standalone.

## `ChartsThemeProvider`

Wrap the section of your app that renders charts:

```tsx
import { ChartsThemeProvider } from '@geti-ai/charts';

export function Dashboard() {
  return (
    <ChartsThemeProvider>
      <MyCharts />
    </ChartsThemeProvider>
  );
}
```

`ChartsThemeProvider` provides a chart theme context consumed by all chart primitives and components. It does **not** require `ThemeProvider` from `@geti-ai/ui`, but should be nested inside it when both are present.

## `useGetiUIChartsTheme`

Use `useGetiUIChartsTheme` to read an adapter theme object from currently available CSS variables:

```tsx preview
import { useGetiUIChartsTheme } from '@geti-ai/charts';

export default function App() {
  const theme = useGetiUIChartsTheme();

  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 8, fontSize: 13 }}>
      <p style={{ color: 'var(--spectrum-global-color-gray-300)', margin: 0 }}>
        Active chart theme tokens:
      </p>
      {Object.entries(theme).slice(0, 8).map(([name, value]) => (
        <div key={name} style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
          <span
            style={{
              width: 16,
              height: 16,
              borderRadius: 3,
              background: typeof value === 'string' ? value : 'var(--spectrum-global-color-gray-500)',
              flexShrink: 0,
            }}
          />
          <code style={{ color: 'var(--spectrum-global-color-gray-200)', fontSize: 12 }}>
            {name}: {typeof value === 'string' ? value : '[object]'}
          </code>
        </div>
      ))}
    </div>
  );
}
```

The returned object is a partial chart theme override (compatible with `ChartsThemeProvider theme={...}`):

```ts
type PartialChartTheme = ReturnType<typeof useGetiUIChartsTheme>;
```

## Using theme colours in custom charts

Pass theme colours directly to Recharts elements:

```tsx preview
import { Line, LineChart as RechartsLineChart } from 'recharts';
import { GetiCartesianGrid, GetiXAxis, GetiYAxis, GetiTooltip, GetiResponsiveContainer } from '@geti-ai/charts/primitives';
import { useGetiUIChartsTheme } from '@geti-ai/charts';

const data = [
  { t: 0, a: 10, b: 20, c: 15 },
  { t: 1, a: 18, b: 17, c: 22 },
  { t: 2, a: 24, b: 14, c: 19 },
  { t: 3, a: 20, b: 21, c: 25 },
  { t: 4, a: 28, b: 25, c: 30 },
];

export default function App() {
  const theme = useGetiUIChartsTheme();
  const series = theme.dataColors ?? ['#4C9BE8', '#5CE6A2', '#F9C846'];
  const [c0, c1, c2] = series;

  return (
    <GetiResponsiveContainer height={240}>
      <RechartsLineChart data={data}>
        <GetiCartesianGrid />
        <GetiXAxis dataKey="t" />
        <GetiYAxis />
        <GetiTooltip />
        {[
          { key: 'a', color: c0 },
          { key: 'b', color: c1 },
          { key: 'c', color: c2 },
        ].map(({ key, color }) => (
          <Line
            key={key}
            type="monotone"
            dataKey={key}
            stroke={color}
            dot={false}
            isAnimationActive={false}
          />
        ))}
      </RechartsLineChart>
    </GetiResponsiveContainer>
  );
}
```

## Overriding theme tokens

Use `ChartsThemeProvider` overrides to customize chart visuals in a scoped region:

```tsx
import { ChartsThemeProvider, LineChart } from '@geti-ai/charts';

export function MyDashboard() {
  return (
    <ChartsThemeProvider
      theme={{
        dataColors: ['var(--spectrum-global-color-magenta-500)', '#4C9BE8', '#5CE6A2'],
        grid: { stroke: 'var(--spectrum-global-color-gray-800)' },
      }}
    >
      <LineChart data={data} series={[{ dataKey: 'value' }]} aria-label="Custom themed chart" />
    </ChartsThemeProvider>
  );
}
```

## Dataset subset palette (train / validation / test)

For dataset split comparisons, use the built-in subset palette utility so your colors stay consistent across all charts.

Default mapping:

- `train` → green
- `validation` / `val` → red
- `test` → yellow

```tsx
import { withDatasetSubsetPalette } from '@geti-ai/charts';

const series = withDatasetSubsetPalette([
  { dataKey: 'train', name: 'Training set' },
  { dataKey: 'val', name: 'Validation set' },
  { dataKey: 'test', name: 'Test set' },
]);
```

You can also customize the palette (and aliases):

```tsx
import { withDatasetSubsetPalette } from '@geti-ai/charts';

const series = withDatasetSubsetPalette(
  [
    { dataKey: 'training' },
    { dataKey: 'validation' },
    { dataKey: 'testing' },
  ],
  {
    palette: {
      train: '#22c55e',
      validation: '#ef4444',
      test: '#f59e0b',
    },
    aliases: {
      train: ['training'],
      validation: ['validation'],
      test: ['testing'],
    },
  }
);
```

### Line chart example (dataset subsets)

```tsx preview
import { LineChart, withDatasetSubsetPalette } from '@geti-ai/charts';

const data = [
  { epoch: 1, train: 1.42, val: 1.50, test: 1.55 },
  { epoch: 2, train: 1.10, val: 1.20, test: 1.28 },
  { epoch: 3, train: 0.87, val: 0.98, test: 1.06 },
  { epoch: 4, train: 0.71, val: 0.82, test: 0.90 },
];

export default function App() {
  const series = withDatasetSubsetPalette([
    { dataKey: 'train', name: 'Training' },
    { dataKey: 'val', name: 'Validation' },
    { dataKey: 'test', name: 'Test' },
  ]);

  return (
    <LineChart
      data={data}
      xAxisKey="epoch"
      showLegend
      series={series}
      aria-label="Train/validation/test loss"
      height={260}
    />
  );
}
```

### Area chart example (dataset subsets)

```tsx preview
import { AreaChart, withDatasetSubsetPalette } from '@geti-ai/charts';

const data = [
  { step: 1, train: 0.45, val: 0.41, test: 0.39 },
  { step: 2, train: 0.51, val: 0.46, test: 0.44 },
  { step: 3, train: 0.62, val: 0.57, test: 0.53 },
  { step: 4, train: 0.71, val: 0.65, test: 0.60 },
];

export default function App() {
  const series = withDatasetSubsetPalette([
    { dataKey: 'train', name: 'Training', fade: true },
    { dataKey: 'val', name: 'Validation', fade: true },
    { dataKey: 'test', name: 'Test', fade: true },
  ]);

  return (
    <AreaChart
      data={data}
      xAxisKey="step"
      showLegend
      series={series}
      aria-label="Train/validation/test IoU"
      height={260}
    />
  );
}
```

### Bar chart example (dataset subsets)

```tsx preview
import { BarChart, withDatasetSubsetPalette } from '@geti-ai/charts';

const data = [
  { split: 'Fold A', train: 0.91, val: 0.85, test: 0.83 },
  { split: 'Fold B', train: 0.93, val: 0.86, test: 0.84 },
  { split: 'Fold C', train: 0.92, val: 0.87, test: 0.85 },
];

export default function App() {
  const series = withDatasetSubsetPalette([
    { dataKey: 'train', name: 'Training' },
    { dataKey: 'val', name: 'Validation' },
    { dataKey: 'test', name: 'Test' },
  ]);

  return (
    <BarChart
      data={data}
      xAxisKey="split"
      showLegend
      series={series}
      aria-label="Train/validation/test per split"
      height={260}
    />
  );
}
```

### Scatter / bubble chart example (dataset subsets)

```tsx preview
import { ScatterChart, withDatasetSubsetPalette } from '@geti-ai/charts';

const train = [
  { x: 2.8e6, y: 0.47, z: 9.8 },
  { x: 1.1e7, y: 0.54, z: 27.0 },
];
const val = [
  { x: 2.8e6, y: 0.44, z: 9.8 },
  { x: 1.1e7, y: 0.50, z: 27.0 },
];
const test = [
  { x: 2.8e6, y: 0.42, z: 9.8 },
  { x: 1.1e7, y: 0.48, z: 27.0 },
];

export default function App() {
  const series = withDatasetSubsetPalette(
    [
      { name: 'Train', data: train, zKey: 'z' },
      { name: 'Validation', data: val, zKey: 'z' },
      { name: 'Test', data: test, zKey: 'z' },
    ],
    { matchBy: 'name' }
  );

  return (
    <ScatterChart
      series={series}
      zAxisProps={{ dataKey: 'z', range: [80, 320], name: 'GFLOPs' }}
      showLegend
      xScale={{ scale: 'log', domain: [1e6, 'auto'] }}
      xAxisLabel="Parameters"
      yAxisLabel="mAP @ 50:95"
      aria-label="Train/validation/test model comparison"
      height={320}
    />
  );
}
```

### Sparkline example (dataset subsets)

```tsx preview
import { Sparkline, getDatasetSubsetColor } from '@geti-ai/charts';

export default function App() {
  return (
    <div style={{ display: 'grid', gap: 10 }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
        <span style={{ width: 80 }}>Train</span>
        <Sparkline
          data={[1.42, 1.10, 0.87, 0.71].map((value) => ({ value }))}
          color={getDatasetSubsetColor('train')}
          area
        />
      </div>
      <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
        <span style={{ width: 80 }}>Validation</span>
        <Sparkline
          data={[1.50, 1.20, 0.98, 0.82].map((value) => ({ value }))}
          color={getDatasetSubsetColor('val')}
          area
        />
      </div>
      <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
        <span style={{ width: 80 }}>Test</span>
        <Sparkline
          data={[1.55, 1.28, 1.06, 0.90].map((value) => ({ value }))}
          color={getDatasetSubsetColor('test')}
          area
        />
      </div>
    </div>
  );
}
```

## Design token mapping

`useGetiUIChartsTheme` maps these Spectrum variables when available:

| Spectrum variable | Chart theme field |
|---|---|
| `--spectrum-global-color-gray-100` | `backgroundColor` |
| `--spectrum-global-color-gray-700` | `typography.color`, `legend.color` |
| `--spectrum-global-color-gray-300` | `grid.stroke` |
| `--spectrum-global-color-gray-400` | `axis.lineColor`, `axis.tickColor`, `tooltip.borderColor` |
| `--spectrum-global-color-gray-200` | `tooltip.backgroundColor` |

## Related

- [Installation](./installation)
- [Overview](./overview)
- [Custom Charts](./custom-charts)
- [Geti UI Theming](/components/theming)
