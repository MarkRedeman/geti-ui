---
title: Donut chart
---

# Donut chart (primitive)

Use Recharts `PieChart` + `Pie` with a non-zero `innerRadius` for full low-level control.

## Props

Key Recharts props used on the `Pie` primitive:

| Prop | Type | Default | Description |
|---|---|---|---|
| `dataKey` | `string` | required | Key in each data object for the slice value |
| `nameKey` | `string` | — | Key used for the slice name in tooltip/legend |
| `innerRadius` | `number \| string` | `0` | Inner hole radius — use `'58%'` or similar for a donut ring |
| `outerRadius` | `number \| string` | `'80%'` | Outer radius of the pie |
| `paddingAngle` | `number` | `0` | Gap in degrees between slices |
| `label` | `boolean \| object \| function` | `false` | Render slice labels |
| `isAnimationActive` | `boolean` | `true` | Disable for static/SSR renders |

## Variant — default

```tsx preview
import { Cell, Pie, PieChart } from 'recharts';
import { GetiLegend, GetiResponsiveContainer, GetiTooltip } from '@geti-ai/charts/primitives';

const data = [
  { name: 'Images', value: 52 },
  { name: 'Videos', value: 18 },
  { name: 'Annotations', value: 22 },
  { name: 'Models', value: 8 },
];

const colors = ['var(--spectrum-global-color-blue-500)', 'var(--spectrum-global-color-green-500)', 'var(--spectrum-global-color-yellow-500)', 'var(--spectrum-global-color-purple-500)'];

export default function App() {
  return (
    <GetiResponsiveContainer height={320}>
      <PieChart>
        <Pie data={data} dataKey="value" nameKey="name" innerRadius="58%" outerRadius="82%" isAnimationActive={false}>
          {data.map((_entry, index) => <Cell key={index} fill={colors[index % colors.length]} />)}
        </Pie>
        <GetiTooltip />
        <GetiLegend />
      </PieChart>
    </GetiResponsiveContainer>
  );
}
```

## Variant — labels + spacing

```tsx preview
import { Cell, Pie, PieChart } from 'recharts';
import { GetiLegend, GetiResponsiveContainer, GetiTooltip } from '@geti-ai/charts/primitives';

const data = [
  { name: 'Train', value: 70 },
  { name: 'Validation', value: 20 },
  { name: 'Test', value: 10 },
];

const colors = ['#22c55e', '#ef4444', '#f59e0b'];

export default function App() {
  return (
    <GetiResponsiveContainer height={320}>
      <PieChart>
        <Pie
          data={data}
          dataKey="value"
          nameKey="name"
          innerRadius="60%"
          outerRadius="82%"
          paddingAngle={2}
          label
          isAnimationActive={false}
        >
          {data.map((_entry, index) => <Cell key={index} fill={colors[index % colors.length]} />)}
        </Pie>
        <GetiTooltip />
        <GetiLegend />
      </PieChart>
    </GetiResponsiveContainer>
  );
}
```

## Variant — thick ring + no legend

```tsx preview
import { Cell, Pie, PieChart } from 'recharts';
import { GetiResponsiveContainer, GetiTooltip } from '@geti-ai/charts/primitives';

const data = [
  { name: 'Queued', value: 12 },
  { name: 'Running', value: 6 },
  { name: 'Completed', value: 52 },
];

const colors = ['#f59e0b', '#60a5fa', '#22c55e'];

export default function App() {
  return (
    <GetiResponsiveContainer height={320}>
      <PieChart>
        <Pie data={data} dataKey="value" nameKey="name" innerRadius="42%" outerRadius="86%" isAnimationActive={false}>
          {data.map((_entry, index) => <Cell key={index} fill={colors[index % colors.length]} />)}
        </Pie>
        <GetiTooltip />
      </PieChart>
    </GetiResponsiveContainer>
  );
}
```

## Series highlighting

This chart supports interactive highlighting. See [Highlights](/charts/highlights).

Related: [Donut chart (composition)](/charts/compositions/donut-chart)
