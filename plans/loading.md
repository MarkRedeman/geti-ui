# Loading

We have a few loading components.
I'd like to consolidate the `Loading` nad `IntelBrandedLoading` component into 1, where we will have `variant='spinner'` or `variant='intel'`.

The progress circle should stay as is as, like the progressbar it could be used for something else.

