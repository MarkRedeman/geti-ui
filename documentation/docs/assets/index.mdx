---
title: Assets
---

# Assets

`@geti/ui` ships production-ready SVG icons, illustration images, domain visuals, and primary-tool thumbnails as static assets.

## Subpath imports

Each asset group is available as a standalone subpath so consumers only pay for what they use:

| Subpath | Contents |
|---|---|
| `@geti/ui/icons` | Custom SVG icons + Adobe Spectrum workflow re-exports |
| `@geti/ui/assets/images` | Illustration SVGs and loading/background images (webp) |
| `@geti/ui/assets/domains` | Domain-type SVG thumbnails (classification, detection, …) |
| `@geti/ui/assets/primary-tools` | Annotation tool preview images (webp) |
| `@geti/ui/assets` | Namespace re-export of all the above |

## Quick start

```tsx
// SVG icon rendered as a React component
import { Add, Delete } from '@geti/ui/icons';

// Illustration image
import { WorkInProgressIcon } from '@geti/ui/assets/images';

// webp URL (use in <img src={…}>)
import { LoginBackground } from '@geti/ui/assets/images';

// Domain thumbnail
import { DetectionImg } from '@geti/ui/assets/domains';

// Tool thumbnail URL
import { BrushTool } from '@geti/ui/assets/primary-tools';
```

## See also

- [Icons](/assets/icons) — full icon catalogue
- [Images](/assets/images) — illustration and image assets
- [Domains](/assets/domains) — domain-type visuals
- [Primary Tools](/assets/primary-tools) — annotation tool thumbnails
