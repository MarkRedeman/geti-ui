---
title: ColorArea
---

import { ColorArea, Flex } from '@geti/ui';
import { ExampleCard } from '../../../../theme/ExampleCard';

# ColorArea

`ColorArea` renders a two-dimensional gradient surface that lets users adjust two color channels simultaneously by dragging a thumb. Use it as the main color-picking surface in a color editor.

```tsx
import { ColorArea } from '@geti/ui';
```

- [React Spectrum ColorArea ↗](https://react-spectrum.adobe.com/react-spectrum/ColorArea.html)
- [Geti UI Theming](/components/theming)

## Examples

<ExampleCard
  title="Default (red + green channels)"
  code={`<ColorArea defaultValue="#7f0000" />`}
>
  <ColorArea defaultValue="#7f0000" />
</ExampleCard>

<ExampleCard
  title="HSB — saturation/brightness"
  code={`<Flex gap="size-200">
  <ColorArea defaultValue="hsb(0, 100%, 50%)" xChannel="saturation" yChannel="brightness" />
  <ColorArea defaultValue="hsb(240, 100%, 50%)" xChannel="saturation" yChannel="brightness" />
</Flex>`}
>
  <Flex gap="size-200">
    <ColorArea defaultValue="hsb(0, 100%, 50%)" xChannel="saturation" yChannel="brightness" />
    <ColorArea defaultValue="hsb(240, 100%, 50%)" xChannel="saturation" yChannel="brightness" />
  </Flex>
</ExampleCard>

<ExampleCard
  title="Custom size"
  code={`<ColorArea defaultValue="#7f0000" size={200} />`}
>
  <ColorArea defaultValue="#7f0000" size={200} />
</ExampleCard>

## Responsibility

`ColorArea` is a direct pass-through of React Spectrum's `ColorArea`. No defaults are changed.

## Variants and states

| Feature | Prop | Notes |
|---|---|---|
| Channels | `xChannel` + `yChannel` | Determines axes; defaults depend on the color format |
| Size | `size` | Width and height in pixels |
| Disabled | `isDisabled` | Non-interactive |

## Props (deep dive)

### Value / state

- `value` / `defaultValue`: any CSS color string or `Color` object.
- `xChannel`: the color channel mapped to the horizontal axis.
- `yChannel`: the color channel mapped to the vertical axis.
- `size`: number — width and height of the square area in pixels.
- `isDisabled`.

### Events

- `onChange(color)`: fires during drag.
- `onChangeEnd(color)`: fires on release.

### Accessibility

- Provides a two-dimensional `role="slider"` interaction.
- Arrow keys move the thumb one step; Shift+Arrow moves by larger increments.

## Related components

- `ColorSlider` — single-channel slider for precise channel control.
- `ColorWheel` — hue-only circular picker often paired with `ColorArea`.
- `ColorPickerDialog` — full-featured dialog using `ColorArea` as the main surface.

## Example use cases

<details>
  <summary>Typical use cases</summary>

- Main color picker surface in the `ColorPickerDialog` (saturation × brightness).
- Embedded color editor inside an annotation drawing toolbar.
- Custom color property editor in a design-oriented settings panel.

</details>
