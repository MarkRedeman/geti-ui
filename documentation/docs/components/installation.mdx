# Installation

This guide explains how to install and start using `@geti/ui` in a React application.

## Requirements

- Node.js 24+
- A React project (React 18+)
- A package manager (`pnpm`, `npm`, or `yarn`)

## Install the package

```bash
pnpm add @geti/ui
```

or

```bash
npm install @geti/ui
```

or

```bash
yarn add @geti/ui
```

## Wrap your app with `ThemeProvider`

`@geti/ui` components require both:

1. the package stylesheet (design tokens + component styles), and
2. `ThemeProvider` at app root.

```tsx
import '@geti/ui/styles.css';
```

Then wrap your app once at the root:

```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import '@geti/ui/styles.css';
import { ThemeProvider } from '@geti/ui';
import { App } from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <ThemeProvider>
    <App />
  </ThemeProvider>
);
```

## Use components

```tsx
import { Button } from '@geti/ui';

export function Example() {
  return <Button variant="accent">Click me</Button>;
}
```

## Next steps

- Read the [Theming](/components/theming) guide
- Browse component documentation in the sidebar
