name: CI

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]

concurrency:
    group: ci-${{ github.ref }}
    cancel-in-progress: true

permissions: {}

jobs:
    docs:
        name: Docs checks
        runs-on: ubuntu-latest
        permissions:
            contents: read
        steps:
            - uses: actions/checkout@v6
              with:
                  persist-credentials: false

            - uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: npm

            - name: Install dependencies
              run: npm ci

            - name: Documentation coverage check
              run: npm run docs:check:coverage

            - name: Documentation no-stories-imports check
              run: npm run docs:check:no-stories-imports

            - name: Build documentation
              run: npm run docs:build

    ci:
        name: Build & Test
        runs-on: ubuntu-latest
        permissions:
            contents: read
        steps:
            - uses: actions/checkout@v6
              with:
                  persist-credentials: false

            - uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: npm

            - name: Install dependencies
              run: npm ci

            - name: Type check
              run: npm run type-check
              working-directory: packages/ui

            - name: Type check (smart-tools)
              run: npm run type-check
              working-directory: packages/smart-tools

            - name: Format check
              run: npm run format:check
              working-directory: packages/ui

            - name: Unit tests
              run: npm run test
              working-directory: packages/ui

            - name: Unit tests (smart-tools)
              run: npm run test
              working-directory: packages/smart-tools

            - name: Build library
              run: npm run build
              working-directory: packages/ui

            - name: Build library (smart-tools)
              run: npm run build
              working-directory: packages/smart-tools

    playwright:
        name: Playwright E2E (docs)
        runs-on: ubuntu-latest
        needs: [docs, ci]
        permissions:
            contents: read
        steps:
            - uses: actions/checkout@v6
              with:
                  persist-credentials: false

            - uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: npm

            - name: Install dependencies
              run: npm ci

            - name: Install Playwright browsers (docs)
              run: npm exec --workspace=@geti-ai/documentation -- playwright install --with-deps chromium

            - name: Run docs routes smoke test
              run: npm run docs:test:e2e
              env:
                  CI: true

            - name: Upload docs Playwright report
              uses: actions/upload-artifact@v7
              if: always()
              with:
                  name: docs-playwright-report
                  path: documentation/playwright-report
                  retention-days: 7
