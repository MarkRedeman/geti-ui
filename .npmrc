# Pin exact dependency versions when running npm install <pkg>
save-exact=true

# Reject install if a package requires a different Node.js version
engine-strict=true

# Only install packages published at least 3 days ago (supply-chain defense)
min-release-age=3
